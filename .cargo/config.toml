[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=mold"]

[env]
# sqlx offline mode for CI builds without a live database
SQLX_OFFLINE = { value = "true", force = false }
