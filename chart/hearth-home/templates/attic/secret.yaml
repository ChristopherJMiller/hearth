{{- if not .Values.secrets.existingSecret }}
{{- /* The JWT secret is stored in the main hearth secrets Secret. */}}
{{- /* This file is intentionally a no-op when existingSecret is set. */}}
{{- /* The key "attic-token-secret" is expected in the secret named by hearth-home.secretName. */}}
{{- end }}
