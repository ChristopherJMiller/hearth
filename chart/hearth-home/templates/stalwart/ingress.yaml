{{- if and .Values.capabilities.mail .Values.stalwart.hosted .Values.stalwart.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "hearth-home.fullname" . }}-stalwart
  labels:
    {{- include "hearth-home.componentLabels" (dict "context" . "component" "stalwart") | nindent 4 }}
  annotations:
    {{- include "hearth-home.certManagerIngressAnnotation" (dict "context" . "userTls" .Values.stalwart.ingress.tls) | nindent 4 }}
    {{- with .Values.stalwart.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if .Values.stalwart.ingress.className }}
  ingressClassName: {{ .Values.stalwart.ingress.className }}
  {{- end }}
  {{- include "hearth-home.ingressTls" (dict "context" . "userTls" .Values.stalwart.ingress.tls "hosts" .Values.stalwart.ingress.hosts "component" "stalwart") | nindent 2 }}
  rules:
    {{- range .Values.stalwart.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "hearth-home.fullname" $ }}-stalwart
                port:
                  name: http
          {{- end }}
    {{- end }}
{{- end }}
